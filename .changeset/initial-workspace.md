---
"@eyelock-assistants/gitflow": patch
---

Adopt pnpm workspaces + changesets for per-package versioning. `plugin.json` / `.harness.json` remain the single source of truth; `package.json` mirrors are generated by `scripts/sync-manifests.mjs`.
