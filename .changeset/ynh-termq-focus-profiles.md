---
"@eyelock-assistants/ynh-dev": minor
"@eyelock-assistants/termq-dev": minor
---

Add `profiles` and `focuses` to `ynh-dev` and `termq-dev` sample harnesses. `ynh-dev` gains five Go-specific focuses (code-review, security-hardening, cli-contract, refactor, changelog) and a math-olympiad include; `termq-dev` gains five Swift/SwiftUI focuses (code-review, security-audit, refactor, pr-summary, changelog).
